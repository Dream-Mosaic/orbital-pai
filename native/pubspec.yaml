name: henry_wall
description: "Henry native wall client (Milestone 1a spike + skeleton)."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.0
  stream_channel: ^2.1.2
  record: ^5.1.2
  porcupine_flutter: ^3.0.3
  path_provider: ^2.1.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
